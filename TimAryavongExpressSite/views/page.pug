extends partials/header

block content
    div(class='pageContents lead')
      if (page.title == 'Contact Tim')
        p= page.content
        p email: #{page.email}
        p phone: #{page.phone}
      else
        p= page.content
    include partials/footer